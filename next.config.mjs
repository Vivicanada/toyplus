/** @type {import('next').NextConfig} */
const nextConfig = {
  output: 'export',
  reactStrictMode: true,
  images: {
    unoptimized: true,
  },
  basePath: '/toyplus',
  assetPrefix: '/toyplus/',
};

export default nextConfig;
